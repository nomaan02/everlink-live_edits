{%- liquid
  assign sticky_class = ''
  if section.settings.sticky_header
    assign sticky_class = 'header--sticky'
  endif

  assign logo_width = section.settings.logo_width
-%}

<style>
  #shopify-section-{{ section.id }} .header__logo img {
    width: {{ logo_width }}px;
    height: auto;
  }
</style>

<header class="header {{ sticky_class }}" data-section-id="{{ section.id }}" data-section-type="header">
  <div class="container">
    <div class="header__inner">
      {%- comment -%} Logo {%- endcomment -%}
      <div class="header__logo">
        <a href="{{ routes.root_url }}" title="{{ shop.name }}">
          {%- if section.settings.logo_image != blank -%}
            <img
              src="{{ section.settings.logo_image | image_url: width: logo_width }}"
              srcset="
                {{ section.settings.logo_image | image_url: width: logo_width }} 1x,
                {{ section.settings.logo_image | image_url: width: logo_width, scale: 2 }} 2x
              "
              alt="{{ shop.name }}"
              loading="lazy"
              width="{{ logo_width }}"
              height="auto"
            >
          {%- else -%}
            <span class="header__logo-text">{{ section.settings.logo_text }}</span>
          {%- endif -%}
        </a>
      </div>

      {%- comment -%} Navigation {%- endcomment -%}
      <nav class="header__nav" role="navigation">
        <ul>
          {%- if section.settings.nav_link_1_text != blank -%}
            <li>
              <a href="{{ section.settings.nav_link_1_url | default: '#' }}">
                {{ section.settings.nav_link_1_text }}
              </a>
            </li>
          {%- endif -%}

          {%- if section.settings.nav_link_2_text != blank -%}
            <li>
              <a href="{{ section.settings.nav_link_2_url | default: '#' }}">
                {{ section.settings.nav_link_2_text }}
              </a>
            </li>
          {%- endif -%}

          {%- if section.settings.nav_link_3_text != blank -%}
            <li>
              <a href="{{ section.settings.nav_link_3_url | default: '#' }}">
                {{ section.settings.nav_link_3_text }}
              </a>
            </li>
          {%- endif -%}
        </ul>
      </nav>

      {%- comment -%} Mobile Menu Toggle {%- endcomment -%}
      <button class="header__menu-toggle" aria-label="Toggle menu" aria-expanded="false">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </div>
</header>

{% schema %}
{
  "name": "Header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo_image",
      "label": "Logo image"
    },
    {
      "type": "text",
      "id": "logo_text",
      "label": "Logo text",
      "default": "Everlink"
    },
    {
      "type": "range",
      "id": "logo_width",
      "min": 80,
      "max": 240,
      "step": 10,
      "unit": "px",
      "label": "Logo width",
      "default": 150
    },
    {
      "type": "text",
      "id": "nav_link_1_text",
      "label": "Nav link 1",
      "default": "Our Story"
    },
    {
      "type": "url",
      "id": "nav_link_1_url",
      "label": "Nav link 1 URL"
    },
    {
      "type": "text",
      "id": "nav_link_2_text",
      "label": "Nav link 2",
      "default": "Collections"
    },
    {
      "type": "url",
      "id": "nav_link_2_url",
      "label": "Nav link 2 URL"
    },
    {
      "type": "text",
      "id": "nav_link_3_text",
      "label": "Nav link 3",
      "default": "Contact"
    },
    {
      "type": "url",
      "id": "nav_link_3_url",
      "label": "Nav link 3 URL"
    },
    {
      "type": "checkbox",
      "id": "sticky_header",
      "label": "Sticky header",
      "default": true
    }
  ]
}
{% endschema %}
